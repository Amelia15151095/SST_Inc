import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hi, world!")
            Text("Ahhhhhhhh")
            Text("hi")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
